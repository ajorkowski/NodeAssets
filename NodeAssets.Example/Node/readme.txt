Make sure to install nodejs and then run > npm install in this folder, this will load all the node
dependencies and let you minify js/css and use coffeescript/stylus. If you don't need to do any of
the above it is not mandatory!