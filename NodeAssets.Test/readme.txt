To run the Test suite make sure to install nodejs/npm and then run

> cd Node
> npm install

This will install the required modules into the Node folder. The tests
are configured to run the modules from this folder. (You should replicate this
in your own files).